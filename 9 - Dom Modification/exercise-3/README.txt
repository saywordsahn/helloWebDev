Using the html given, when the button is pressed:
1. Create a list item (HTML elment <li>) using document.createElement()
2. Set the list items innerHtml property to 'Hello'
2. Add this list item as a child of aList using the appendChild() function