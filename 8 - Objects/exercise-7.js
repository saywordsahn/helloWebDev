
// DO NOT MODIFY
class Hero {
    constructor(name, maxHP) {
        this.name = name;
        this.maxHP = maxHP
        this.hp = maxHP;
    }
}

// we now have a class that we can create Hero objects from.
// Create an instance of a hero using the constructor
// - the name 'Highlander'
// - 100 maxHP

// NOTE: creating an instance of a class does not print anything to screen
// so don't expect any output for this exercise